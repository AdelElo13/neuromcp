{
  "$comment": "~/Library/Application Support/Claude/claude_desktop_config.json (macOS) — simplest setup: Desktop spawns its own neuromcp over stdio.",
  "mcpServers": {
    "neuromcp": {
      "command": "npx",
      "args": ["-y", "neuromcp"]
    }
  }
}
